[package]
name = "tictactwo-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
# Compiles the RL policy/value net + play-time MCTS (needs src/rlnet.bin).
rl = []

[profile.release]
opt-level = 3
lto = true
panic = "abort"
codegen-units = 1
strip = true
